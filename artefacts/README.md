# Artefact Folder

The `artefacts/` folder stores external artefacts like software packages, files
etc. that are used to customize the containers.

## Notes

- The contents of this folder are **not versioned** in Git.  
  They are ignored via `.gitignore` to prevent large binary database files from
  being committed.
