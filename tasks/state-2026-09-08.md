# Session State: oracle-free-labs - 2026-09-08

Supersedes `state-2026-09-07.md`.

Verifikationstest "TDE RESTORE AS ENCRYPTED USING KEY" fuer den ALTIMA-Termin.
Beweisfrage: erneuert `RESTORE ... AS ENCRYPTED USING KEY` das Tablespace-Schluessel-
material, oder wickelt es den bestehenden Schluessel nur neu ein?

## Done this session

Keine neue Arbeit in diesem Repo. Repo-Stand unveraendert seit gestern.
Letzter Commit: `7e40e88 docs(tasks): reconcile todo.md with what the run actually proved`.

## Open (P1)

- [ ] **Deck-Slides sperren.** Phase 1 wartet auf Stefans Rueckmeldung pro Slide
      ("passt" / "ueberarbeiten"). Erst danach Phase 3, der pptx-Bau mit den
      Accenture-Buildern. Blockiert auf Entscheid, nicht auf Arbeit.
- [ ] **Manuelles Review der 21 Schritte** durch Stefan, entlang des Runbooks. Er hat es
      angekuendigt; das Runbook ist dafuer vollstaendig.
- [ ] **`extractable=false` bei Oracle klaeren**: unterbindet das Attribut am
      TDE-Master-Key den `okvutil download`, oder verhindert es nur das Endpoint-Caching?
      Die Doku belegt nur Letzteres. Einziger als **offen** markierter Evidenzpunkt im
      Deck (Slide 8) - die konkreteste Frage fuer den Kundentermin.

## Open (P2)

- [ ] **Ablageort des Decks entscheiden**: liegt unter
      `~/Downloads/tde-clone-independence-html/`. Alle Belege liegen im Repo.
- [ ] **`tasks/todo.md` bereinigen**: 62 Checkboxen offen, viele davon erledigt.
      AP1 bis AP10 im Wesentlichen durch, Variante C gemessen.
- [ ] **Zwei ungemessene Punkte**: UNITED gegen ISOLATED (`TDE_CONFIGURATION` in der PDB)
      und `MERGE KEYSTORE` als dokumentierter Weg.

## To resume

```bash
cd /Users/stefan.oehrli/Repos/own/oehrlis/oracle-free-labs
sed -n '1,60p' tasks/e2e-facts.md            # die Messwerte, einzige Zahlenquelle
sed -n '1,40p' doc/tde-e2e-protokoll.md      # Ergebnistabelle des gruenen Laufs
open ~/Downloads/tde-clone-independence-html/brief.html   # Deck Phase 1, wartet auf Lock
./scripts/tde-verify/run_all.sh --list       # die 21 Schritte
```

Lauf wiederholen (loescht beide Services): `./scripts/tde-verify/run_all.sh --delete --yes`
Protokoll neu erzeugen: `./scripts/tde-verify/make_protocol.sh --log <log> --out <md>`
