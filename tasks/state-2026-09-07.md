# Session State: oracle-free-labs - 2026-09-07

Verifikationstest "TDE RESTORE AS ENCRYPTED USING KEY" fuer einen Kundentermin.
Beweisfrage: erneuert `RESTORE ... AS ENCRYPTED USING KEY` das Tablespace-Schluessel-
material, oder wickelt es den bestehenden Schluessel nur neu ein?

## Done this session

- **E2E-Lauf gruen**: ein durchgehender Lauf 00 bis 90, 21 von 21 Schritten, 27 Minuten.
  Log `artefacts/tde-e2e-run-20260906.log`, Protokoll `doc/tde-e2e-protokoll.md`,
  Messwerte `tasks/e2e-facts.md`. Zwei vorherige E2E-Anlaeufe scheiterten - beide an
  inhaltlichen Befunden, nicht an Skriptfehlern (siehe unten).
- **Antwort auf die Kundenfrage steht**: kein RMAN-Weg erneuert den Tablespace-Schluessel.
  A, C, D je 313 von 313 Canary-Bloecken identisch; B1 `ORA-00600`, B2 `ORA-19870`/
  `ORA-28374`. Neues Material nur ueber PDB-Klon, `ONLINE REKEY` oder Discard-Pfad.
- **PDB-Serie 61 bis 69 erstmals ueberhaupt gelaufen** und gruen. Kernbefund: Klon (P1
  lokal, P4 remote) erzeugt neues Schluesselmaterial bei **unveraenderter MASTERKEYID** -
  unter gleichem MEK kann das kein Re-wrap sein. Archiv-Transport (P2) erhaelt Schluessel
  und Chiffrat bitgenau, auch in eine fremde CDB.
- **Zwei Befunde, die je einen E2E-Lauf gekostet haben**:
  verschluesseltes Undo bricht den Discard-Pfad (`ORA-28304` auf dem Undo-Datafile,
  waehrend `V$ENCRYPTED_TABLESPACES` korrekt 0 Zeilen meldet); und ohne Quell-MEK
  oeffnet die Zieldatenbank gar nicht mehr, sie bleibt auf `MOUNTED`.
- **Positivkontrolle repariert**: sie zaehlte Ueberschriftenzeilen statt Bloecke und
  meldete "1 differing / 1 total", waehrend der Vergleich 1140 von 2561 fand. Der Schritt,
  der jedes "identisch" ueberhaupt rechtfertigt, mass nichts.
- **Dokumentation auf die Messwerte gestellt**: `tde-restore-as-encrypted.md` (Vergleich,
  Matrix, Empfehlung neu), `tde-clone-independence.md` (Stufenmodell mit PDB-Wegen),
  `tde-okv-argumentation.md` (Provenance jetzt gemessen statt argumentiert),
  `tde-key-architecture.md` (D-gegen-F als Beleg der dritten Ebene),
  `tde-restore-runbook.md` (Phasen 4a, 5a, 5.10, 6a - alle 21 Schritte haben jetzt eine
  manuelle Entsprechung). 10 Mermaid-Diagramme neu gerendert.
- **Drei widerlegte Doku-Aussagen korrigiert**: Variante C war als "nicht gemessen"
  gefuehrt; "`ONLINE REKEY` ist das einzige Verfahren fuer neues Material" gilt nur fuer
  den Tablespace an Ort und Stelle, nicht fuer den Weg ueber eine Kopie; der dokumentierte
  `KEY_VERSION`-Reset nach Plug-in wurde im Lab **nicht** beobachtet.
- **Speicherlimit** `ODBENC*_DB_MEM` von 3g auf 2g gesenkt, nachdem der OOM-Killer einen
  `sqlplus`-Prozess mitten im Lauf abgeschossen hatte.
- **Deck Phase 1 fertig**: `~/Downloads/tde-clone-independence-html/brief.html`, 8 Slides
  mit Argumentationskette und SVG-Mockups. Slide 8 entstand aus Stefans Rueckfrage zur
  OKV-Topologie.
- **OKV-Recherche belegt**: kein Weg zwischen unabhaengigen OKV ohne Wallet-Datei
  (`okvutil download`/`upload`); direkte Replikation nur im Multi-Master-Cluster, 2 bis 16
  Knoten, ueber Rechenzentren zulaessig, repliziert auch die Audit-Daten.

## Open (P1)

- [ ] **Deck-Slides sperren.** Phase 1 wartet auf Stefans Rueckmeldung pro Slide
      ("passt" / "ueberarbeiten"). Erst danach Phase 3, der pptx-Bau mit den
      Accenture-Buildern. Blockiert auf Entscheid, nicht auf Arbeit.
- [ ] **Manuelles Review der 21 Schritte** durch Stefan, entlang des Runbooks. Er hat es
      angekuendigt; das Runbook ist dafuer jetzt vollstaendig.
- [ ] **`extractable=false` bei Oracle klaeren**: unterbindet das Attribut am
      TDE-Master-Key den `okvutil download`, oder verhindert es nur das Endpoint-Caching?
      Die Doku belegt nur Letzteres. Das ist der einzige als **offen** markierte
      Evidenzpunkt im Deck (Slide 8) - und die konkreteste Frage fuer den Kundentermin.

## Open (P2)

- [ ] **Ablageort des Decks entscheiden**: liegt derzeit unter
      `~/Downloads/tde-clone-independence-html/`. Alle Belege liegen im Repo - wenn es
      dazugehoeren soll, gehoert es dorthin.
- [ ] **`tasks/todo.md` ist veraltet**: 62 Checkboxen offen, viele davon erledigt
      (AP1 bis AP10 im Wesentlichen durch, Variante C gemessen). Einmal durchgehen und
      abhaken, sonst taeuscht die Datei ueber den Stand.
- [ ] **Zwei ungemessene Punkte aus der alten Planung**: UNITED gegen ISOLATED
      (`TDE_CONFIGURATION` in der PDB) und `MERGE KEYSTORE` als dokumentierter Weg.

## To resume

```bash
cd /Users/stefan.oehrli/Repos/own/oehrlis/oracle-free-labs
sed -n '1,60p' tasks/e2e-facts.md            # die Messwerte, einzige Zahlenquelle
sed -n '1,40p' doc/tde-e2e-protokoll.md      # Ergebnistabelle des gruenen Laufs
open ~/Downloads/tde-clone-independence-html/brief.html   # Deck Phase 1, wartet auf Lock
./scripts/tde-verify/run_all.sh --list       # die 21 Schritte
```

Lauf wiederholen (loescht beide Services): `./scripts/tde-verify/run_all.sh --delete --yes`
Protokoll neu erzeugen: `./scripts/tde-verify/make_protocol.sh --log <log> --out <md>`
