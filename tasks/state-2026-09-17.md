# Session State: oracle-free-labs - 2026-09-17

Supersedes `state-2026-09-16.md`. Die Session begann am 16.09. und lief ueber
Mitternacht; die Diagramm- und Deck-Befunde stehen ausfuehrlich in der aelteren
Datei und werden hier nicht wiederholt. **Neu gegenueber 09-16:** die
Management-Fassung des Diagramms und die Umstellung der Detailfassungen auf hidden.

**Am Code und an den Skripten des Repos wurde nichts geaendert.** Gelesen wurden
`tasks/e2e-facts.md`, `artefacts/tde-e2e-run-20260906.log` und
`scripts/tde-verify/tests/70_variant_g.sh`. Geschrieben wurden nur die zwei
state-Dateien. Die eigentliche Arbeit lag im Deliverable-Ordner
`NDA-Bank/30_Delivery/`, der nicht Teil dieses Repos ist.

## Done this session

- **ACT-004 geliefert.** Aus einem Diagramm wurden **drei**, alle Querformat, je
  `.mmd` / `.svg` / `.png` in `30_Delivery/`:

  | Datei | Zielgruppe | Ratio |
  | --- | --- | --- |
  | `altima-clone-key-flow-overview.mmd` | Management / Sales: drei Stages, Ampel pro Schluessel, kein Ablauf | 4.6 |
  | `altima-clone-key-flow.mmd` | generisch: Klon-Ablauf, Key-Zustand je Schritt, OKV **oder** Software-Keystore | 5.0 |
  | `altima-clone-key-flow-rman.mmd` | konkret: ZDLRA -> RMAN DUPLICATE -> Acceptance mit den gemessenen Zahlen | 6.4 |

- **Deck v8 ausgeliefert**: 36 Slides, 19 sichtbar, 17 hidden. Overview als
  **Pos 29 sichtbar**, die zwei detaillierten als **Pos 30 und 31 hidden** -
  dasselbe Backup-Muster wie die uebrigen 15. Review-PDF daneben
  (`altima-hld-tde-okv-slides-v8-review.pdf`, Overview ist Seite 17).
  Verifiziert am geschriebenen `.pptx`, nicht am Build.
- **Der Befund, der die Diagramme inhaltlich veraendert hat**: die einzige
  Entzugsmessung ist gescheitert (Step 90, `tde-e2e-run-20260906.log:7470` -
  `DEPENDENT`, `MOUNTED`, `ORA-28374`). Ein Online Rekey des verschluesselten
  Tablespace macht die DB **nicht** unabhaengig vom Quell-MEK. Welches Objekt
  weiter darauf verweist, ist **nicht isoliert** - Kandidat, kein Befund.
  Details in `state-2026-09-16.md`.
- **Nach Stefans Feedback** ("zu komplex fuer Manager / Sales / Architekten") die
  Management-Fassung gebaut und die Detailfassungen auf hidden gestellt. Die
  Overview ist bewusst **fuer den Excalidraw-Import** geschrieben: keine
  Backtick-Markdown-Strings, keine unsichtbaren Links, kein `linkStyle`, keine
  HTML-Entities. Excalidraws Konverter verwirft `classDef` - die Farbabsicht
  steht darum als Kommentar in der Datei.
- **Farbregel korrigiert**: gruen = die Stage haelt ihren eigenen Schluessel,
  rot = der Schluessel ist PRODs, grau = PROD selbst. In der Erstfassung war PROD
  dunkelgruen, also in der Farbe fuer "unabhaengig", obwohl es beide
  Quellschluessel haelt.
- **Zwei Hilfsmittel dauerhaft im Delivery-Ordner abgelegt**, damit der Weg diese
  Session ueberlebt: `altima-diagram-watch.sh` (OraDBA-Header, `shellcheck`
  clean, rendert alle drei Diagramme auf Speichern, `--once` / `--dry-run`) und
  `altima-deck-insert-diagram-slides.py` (setzt auch das `show="0"`-Flag).
- `projects/altima.md` und die ALTIMA-Zeile in `~/notes/state.md` nachgezogen,
  inklusive `Stand:`-Datum. Dabei zwei **vorbestehende** MD055-Fehler in
  `state.md` (Zeilen 54 und 130, schon in `HEAD`) mitkorrigiert.
- Commit `7e90c12` (state-2026-09-16.md), **nicht gepusht**.

## Open (P1)

- [ ] **Stefans Review der Diagramme.** Offen und **ungeklaert**: der
  Entzugs-Kasten steckt jetzt nur noch in den hidden Slides. Ob die Aussage in
  knapper Form auf die sichtbare Overview gehoert, ist **vertagt, nicht
  beantwortet**. Weitere Punkte in `state-2026-09-16.md` (Kasten
  `constraint to settle first` ist eine Folgerung, keine Messung; Titel und
  Lead-Zeilen nicht abgestimmt).
- [ ] **Entscheiden: welche der drei Diagramme gehen in den HLD-Block?**
  Blockiert den SharePoint-Uebertrag mit.
- [ ] **Entscheiden: kommt Pos 28 (Key-Position) in den HLD-Block?** Blockiert
  den Uebertrag inhaltlich - ohne sie laeuft der Kommentar auf HLD 81 ins Leere.
- [ ] **`e2e-facts.md` deckt `run_20260911_093022` nicht ab**, obwohl
  Kundenslides auf diesen Messungen stehen. Die Logs liegen in `artefacts/`,
  **kein Lab-Lauf noetig** - reine Schreibarbeit. Verletzt bis dahin die
  Repo-Regel "keine Zahl ohne Beleg in der Faktenquelle".
- [ ] **`DEC-004` und `DEC-007` gegenlesen** - beide nennen Stefan als Owner
  (due 2026-09-18).
- [ ] **WBS-Nummer nachfassen**, rueckdatiert ab 2026-09-08 gueltig.

## Open (P2)

- [ ] **Auxiliary-Kette end-to-end messen** - weiter offen, nicht angefangen.
- [ ] **Offen und nicht gemessen**: ob ein Rekey **aller** verschluesselten
  Tablespaces plus Database Key den Entzug des Quell-MEK traegt. Bis dahin ist
  Stufe 2 des Stufenmodells nicht belegt.
- [ ] Zwei untracked Verzeichnisse in `artefacts/` seit Sessionbeginn
  (`failure-step50-20260910_170455/`, `manual-run-20260910/`) - nicht von dieser
  Session, Entscheid ueber Tracking oder Loeschen steht aus.

## To resume

```bash
cd /Users/stefan.oehrli/Repos/own/oehrlis/oracle-free-labs
sed -n '1,60p' tasks/state-2026-09-16.md   # der Entzugs-Befund im Detail
open "$HOME/Library/CloudStorage/OneDrive-Accenture/20_Customers/Active/NDA-Bank/30_Delivery/altima-hld-tde-okv-slides-v8-review.pdf"
```

Diagramm anpassen und Deck neu bauen:

```bash
cd "$HOME/Library/CloudStorage/OneDrive-Accenture/20_Customers/Active/NDA-Bank/30_Delivery"
./altima-diagram-watch.sh            # rendert alle drei auf Speichern, Ctrl-C beendet
sed -n '1,25p' altima-deck-insert-diagram-slides.py   # Usage fuer den v8-Neubau
```
