# Session State: oracle-free-labs - 2026-09-14

Supersedes `state-2026-09-11.md`.

Diese Session hat **nichts am Repo geaendert**. Die Arbeit lag vollstaendig auf der
Deliverable-Seite (OneDrive, ausserhalb dieses Repos) plus einer Dokumentations-
Recherche. Der Stand des Repos ist unveraendert der von `v1.3.0`.

## Done this session

- **OKV-Recherche, zwei Architekturfragen belegt** (Administrator's Guide und
  Installation and Upgrade Guide 21.15, als PDF geholt und durchsucht):
  - **Kein separates Netzwerk-Interface fuer Endpoint-Verkehr.** Zwei NICs nur als
    Bond hinter *einer* IP (Dual NIC mode, ab 21.x). Keine Bindung von Diensten an
    Interfaces; konfigurierbar sind laut Doku nur SSH 22 und der TCPS-Listener
    1522. Trennung Admin/Endpoint geht nur ueber Quell-IP-Listen plus Firewall.
    Dual NIC ist ein Installationsentscheid **vor** der Cluster-Induktion, die
    Node-IP danach unveraenderlich (Admin Guide 18.3.2.1, 19.7.1).
  - **Kein Load Balancer.** Oracle erwaehnt in beiden Guides keinen - null Treffer
    fuer "load balancer", "virtual IP", "reverse proxy". Verteilung und Failover
    macht die Endpoint Node Scan List (3.6.3, 3.3.3, 3.5.3). Ein LB waere nicht nur
    redundant, sondern schaedlich: die Scan-Liste pinnt pro Adresse einen Node-DN,
    und die CA-Rotation leitet den Endpoint auf einen *bestimmten* Node um (21.9).
  - Nebenbefund: Oracle empfiehlt fuer grosse Deployments mindestens vier Nodes
    **pro Rechenzentrum** (3.5.3), und Endpoints muessen bei der Enrolment bewusst
    ueber die Nodes verteilt werden (19.4.12).
- Kundenartefakte (OneDrive): Entscheid-Slide und Tenancy-Slide erstellt, Deck von
  29 auf 31 gebaut und gerendert geprueft, Praesentations- und HLD-Auswahl
  dokumentiert. Details bleiben bewusst ausserhalb dieses Repos.
- Ein Schaerfungspunkt, der auch fuer das Lab gilt: **kein Cache und
  `extractable=FALSE` schuetzen den Speicher nicht.** Beides verhindert Persistenz
  und Extraktion aus dem Key Vault, nicht den Zugriff auf SGA und PGA.

## Open (P1)

- [ ] Auxiliary-Kette end-to-end messen: `45_zdlra_aux_pdb_clone.sh` ist lauffaehig,
      die Kette Backup -> Auxiliary-Restore -> PDB-Klon aber **unverifiziert**. Die
      wertvollste offene Messung, weil die Backup-Appliance die bevorzugte Quelle ist.
- [ ] Entscheiden, was mit den zwei ungetrackten Diagnoseverzeichnissen passiert:
      `artefacts/failure-step50-20260910_170455/` und `artefacts/manual-run-20260910/`.
      Ein Sammel-`rm -rf` wurde am 2026-09-10 verweigert - der Entscheid steht
      weiterhin beim User, deshalb liegt der Baum nicht sauber.
- [ ] Belegstelle nachtragen, dass die non-CDB-Architektur in aktuellen Releases
      desupported ist. Steht aktuell als `[TO VERIFY]` auf einer Slide; eine
      Aussage auf einer Slide ohne Fundstelle ist hier nicht zulaessig.

## Open (P2)

- [ ] UNITED gegen ISOLATED messen und `MERGE KEYSTORE` einmal durchspielen - beides
      unverandert ungemessen, in `tasks/todo.md` Zeile 483 und 492.
- [ ] `v1.2.1` neu taggen: der Tag zeigt auf den Commit, der den markdownlint-Fehler
      noch trug. Korrigiert wurde er erst in `bbec57b`.
- [ ] Oracle-19c-Error-Correction-Termine belegen. Angeboten, nicht ausgefuehrt -
      der Punkt liegt bei einem Dritten, nicht bei uns.

## To resume

```bash
cd /Users/stefan.oehrli/Repos/own/oehrlis/oracle-free-labs
git log --oneline -5 && git status -s
sed -n '1,60p' tasks/state-2026-09-11.md      # der Befund, der die Empfehlung umstellte
sed -n '/Einstiegspunkte/,/^## /p' doc/tde-restore-runbook.md | head -60
./scripts/tde-verify/run_all.sh --only 45 --yes   # die offene Auxiliary-Kette
```
